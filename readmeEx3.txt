Lab 3

Representation of Types:

We represent types as an enum of int,bool,class and array.

Subtypes: 

We check if both inputs(a,b) are of type class. If they are we check in the class list if a extends b or recursively if a extends another class that extends b. 